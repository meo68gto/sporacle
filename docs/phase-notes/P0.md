# P0 notes

Assumed: signed-cookie allowlist auth instead of live Supabase Auth, so verify/e2e run without Docker. Roles are viewer/analyst/admin. PGlite stands in for local Postgres.

Flag: spec asked for Supabase CLI local. Schema is Drizzle `pg-*`; swapping the driver to supabase-js + hosted Postgres is a later ops change.

Ambiguity: I1 forbids a column named `name`; DATA_CONTRACT `source.name` is stored as `label`.

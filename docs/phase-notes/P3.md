# P3 notes

Variance ledger uses closed (not status-total) for D3 so $9,996 appears. Promotion writes `measure_definition` + audit; it is not a config toggle. Interpreted “migration-backed” as a durable signed row, not a generated SQL file per click.
